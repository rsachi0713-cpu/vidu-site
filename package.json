{
  "name": "vidu",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "vite": "^8.0.10",
    "wrangler": "^4.87.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.3",
    "aos": "^2.3.4"
  }
}
